[project]
name = "dictate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.120.0",
    "openvino>=2026.2.1",
    "optimum-intel>=2.0.0",
    "python-dotenv>=1.2.2",
    "setuptools<81",
    "sounddevice>=0.5.5",
    "webrtcvad>=2.0.10",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
